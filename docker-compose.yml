services:
  tracker:
    image: ${DOCKER_IMAGE:-pokevault}:${DOCKER_TAG:-latest}
    build: .
    ports:
      - "8765:8765"
    volumes:
      - ./data:/app/data
    environment:
      - TRACKER_HOST=0.0.0.0
      - TRACKER_PORT=8765
    restart: unless-stopped
